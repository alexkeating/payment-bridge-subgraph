type Bridge @entity {
  id: ID!
  count: BigInt!
  bridgeOwner: Bytes! # address
  bridge: Bytes! # address
  payment: [Payment!] @derivedFrom(field: bridge)
}

type Payment @entity {
  id: ID!
  tokenAddress: Bytes!
  payee: Bytes!
  recipient: Bytes!
  bridge: Bytes!
}
